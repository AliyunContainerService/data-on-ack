apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "admin-ui.fullname" . }}
  labels:
    {{- include "admin-ui.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "admin-ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "admin-ui.selectorLabels" . | nindent 8 }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ include "admin-ui.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if not .Values.dashboard.createWebApp }}
          args:
            - --disable-oauth
          {{- end }}
          env:
            - name: MY_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: DASHBOARD_INGRESS_ENABLE
              value: "{{ .Values.dashboard.ingress.enabled }}"
            {{- if .Values.dashboard.ingress.enabled }}
            - name: DASHBOARD_HOST
              value: "{{ (index .Values.dashboard.ingress.hosts 0).host }}"
            {{- end }}
            - name: DASHBOARD_ADMINUID
              value: "{{ .Values.dashboard.adminUid }}"
            - name: INTL_ACCOUNT
              value: "{{ .Values.dashboard.intlAccount }}"
            - name: CREDENTIAL_MODE
              value: "{{ .Values.dashboard.credentialMode }}"
            - name: AK_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: ai-dashboard-credentials
                  key: accessKeyId
                  optional: true
            - name: AK_ACCESS_KEY_SECRET
              valueFrom:
                secretKeyRef:
                  name: ai-dashboard-credentials
                  key: accessKeySecret
                  optional: true
            - name: OIDC_PROVIDER_ARN
              value: "{{ .Values.credentials.rrsa.oidcProviderArn }}"
            - name: OIDC_TOKEN_FILE
              value: "{{ .Values.credentials.rrsa.oidcTokenFile }}"
            - name: FRONTEND_DIR
              value: "/app/dist"
            {{- if .Values.auth.existingSecret }}
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.auth.existingSecret }}
                  key: sessionSecret
            {{- end }}
            - name: ENABLE_SECURE_COOKIE
              value: "{{ .Values.auth.secureCookie }}"
            {{- if .Values.auth.oauthSigninURL }}
            - name: OAUTH_SIGNIN_URL
              value: "{{ .Values.auth.oauthSigninURL }}"
            {{- end }}
            {{- if .Values.auth.oauthTokenURL }}
            - name: OAUTH_TOKEN_URL
              value: "{{ .Values.auth.oauthTokenURL }}"
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.containerPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            timeoutSeconds: 5
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            timeoutSeconds: 5
            periodSeconds: 15
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
